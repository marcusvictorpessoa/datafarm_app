import {StyleSheet} from 'react-native';
import { Colors } from '../../themes/colors';

export const SynchronizeStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: Colors.white,
    padding: 5,
    justifyContent: 'space-around',
    alignItems: 'center',
  },
});
