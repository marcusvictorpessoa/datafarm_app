import {StyleSheet} from 'react-native';
import {Colors} from '../../themes/colors';

export const SynchronizeInfoStyles = StyleSheet.create({
  card: {
    justifyContent: 'space-around',
    alignItems: 'center',
    width: '80%',
    height: '80%',
  },
  numberRegs: {
    fontSize: 18,
    color: Colors.quickSilver,
  },
});
