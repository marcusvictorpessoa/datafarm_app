import {StyleSheet} from 'react-native';
import {Colors} from '../../themes/colors';

export const NoteInputStyles = StyleSheet.create({
  noteInput: {
    borderWidth: 1,
    borderRadius: 5,
    borderColor: Colors.lightGray,
    marginTop: 10,
    marginBottom: 30,
    color: Colors.black,
    padding: 10,
    fontSize: 17,
  },
});
